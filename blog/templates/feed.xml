<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <id>{{ site_url }}{{ base_path }}feed.xml</id>
  <title>{{ site_title }}</title>
  {% if site_description %}<subtitle>{{ site_description }}</subtitle>{% endif %}
  <link rel="alternate" type="text/html" href="{{ site_url }}{{ base_path }}" />
  <link rel="self" type="application/atom+xml" href="{{ site_url }}{{ base_path }}feed.xml" />
  <updated>{{ feed_posts[0].created_at if feed_posts else "1970-01-01T00:00:00Z" }}</updated>
  <author><name>{{ profile_name }}</name></author>
  {% for post in feed_posts %}
  <entry>
    <id>{{ site_url }}{{ post.post_url }}</id>
    <title>{{ post.title }}</title>
    <link rel="alternate" type="text/html" href="{{ site_url }}{{ post.post_url }}" />
    <updated>{{ post.created_at }}</updated>
    <author><name>{{ post.author }}</name></author>
    {% if post.body_html %}<content type="html">{{ post.body_html }}</content>
    {% elif post.excerpt %}<summary>{{ post.excerpt }}</summary>{% endif %}
    {% for label in post.labels %}<category term="{{ label }}" />
    {% endfor %}
  </entry>
  {% endfor %}
</feed>
